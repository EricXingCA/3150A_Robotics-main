/*Defines the macro HELPER_FUNCTIONS_H as part of an include guard.
Used with #ifndef HELPER_FUNCTIONS_H at the top and #endif at the bottom of the header file.
Prevents the contents of this header from being included multiple times during compilation, avoiding redefinition errors.
*/
#ifndef HELPER_FUNCTIONS_H
#define HELPER_FUNCTIONS_H

extern double get_dist_travelled(double degreesTravelled);


#endif